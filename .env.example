# Database (Drizzle + libSQL)
DATABASE_URL=file:./data/social-manager.db

# Better Auth (generate secret: openssl rand -base64 32)
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:5173
# Optional: extra origins (e.g. other dev port), comma-separated
# BETTER_AUTH_TRUSTED_ORIGINS=http://localhost:5174

# Self-hosted: set to true to disable billing and grant all features.
# When true, public registration is disabled after the first user (admin) registers, unless overridden below.
# SELF_HOSTED=false

# When SELF_HOSTED=true: keep registration open after the first user (default: registration closes after first user)
# ALLOW_PUBLIC_REGISTRATION_AFTER_FIRST_USER=true

# Cron: secret for /api/cron/publish-scheduled (e.g. curl "https://yourapp.com/api/cron/publish-scheduled?secret=YOUR_SECRET" every minute)
# CRON_SECRET=

# OAuth Quick Connect (app's credentials; optional)
# Pre každú platformu: PLATFORM_CLIENT_ID + PLATFORM_CLIENT_SECRET.
# Ak nie sú nastavené, používatelia môžu použiť Advanced a vlastné API kľúče.
# X (Twitter) – Developer Portal → Project → OAuth 2.0 Client ID/Secret
# X_CLIENT_ID=
# X_CLIENT_SECRET=
# LinkedIn – Developer App → Auth → Client ID/Secret
# LINKEDIN_CLIENT_ID=
# LINKEDIN_CLIENT_SECRET=
# Facebook / Instagram – Meta for Developers → App → Facebook Login + Instagram Basic
# (môžu zdieľať jednu Meta aplikáciu)
# FACEBOOK_CLIENT_ID=
# FACEBOOK_CLIENT_SECRET=
# INSTAGRAM_CLIENT_ID=
# INSTAGRAM_CLIENT_SECRET=
